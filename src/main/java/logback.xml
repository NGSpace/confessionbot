<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration>

<configuration>
  <import class="ch.qos.logback.classic.encoder.PatternLayoutEncoder"/>
  <import class="ch.qos.logback.core.FileAppender"/>
  <import class="ch.qos.logback.core.ConsoleAppender"/>
	
	<timestamp key="timestamp" datePattern="yyyy_MM_dd"/>
	<timestamp key="hour" datePattern="hh"/>
	<timestamp key="min" datePattern="mm"/>
	<timestamp key="sec" datePattern="ss"/>
	
  <appender name="FILE" class="FileAppender">
    <file>logs/${timestamp}_${hour}_${min}_${sec}.log</file>
    <encoder class="PatternLayoutEncoder">
      <pattern>[%date{dd-MM-yy HH:mm:ss.SSS}] %level [%thread] [%file:%line] - %msg\n</pattern>
    </encoder>
  </appender>

  <appender name="STDOUT" class="ConsoleAppender">
    <encoder class="PatternLayoutEncoder">
      <pattern>%blue([%date{dd-MM-yy HH:mm:ss.SSS}]) %red(%level) %cyan([%thread] [%file:%line]) - %white(%msg)\n</pattern>
    </encoder>
  </appender>

  <root level="debug">
    <appender-ref ref="FILE"/>
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>